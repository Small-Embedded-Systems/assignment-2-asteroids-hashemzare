/*
	This header allows the asteroids to spawn randomly
	It defines where it can travel to (randrange)
	And it defines how it travels across the screen (radians)
*/

int randrange(int from, int to);
float radians(float a);
